Only in AFLplusplus/src: README.md
Only in AFLplusplus/src: afl-analyze.c
Only in AFLplusplus/src: afl-as.c
Only in AFLplusplus/src: afl-cc.c
Only in AFLplusplus/src: afl-common.c
Only in AFLplusplus/src: afl-forkserver.c
Only in AFLplusplus/src: afl-fuzz-bitmap.c
Only in AFLplusplus/src: afl-fuzz-cmplog.c
Only in AFLplusplus/src: afl-fuzz-extras.c
Only in AFLplusplus/src: afl-fuzz-init.c
diff --color -ru AFLplusplus/src/afl-fuzz-mutators.c afl-mm-depend/src/afl-fuzz-mutators.c
--- AFLplusplus/src/afl-fuzz-mutators.c	2024-11-02 13:27:38
+++ afl-mm-depend/src/afl-fuzz-mutators.c	2024-11-02 13:38:55
@@ -406,7 +406,7 @@
   } else {
 
     OKF("Found 'afl_custom_post_run'.");
-
+    mutator->afl_custom_end_job = dlsym(dh, "afl_custom_end_job");
   }
 
   /* "afl_custom_queue_new_entry", optional */
@@ -654,3 +654,9 @@
 
 }
 
+void write_custom_mutator_data(afl_state_t *afl) {
+  if (afl->custom_mutators_count) {
+    LIST_FOREACH_CLEAR(&afl->custom_mutator_list, struct custom_mutator,
+                       { el->afl_custom_end_job(el->data); });
+  }
+}
\ No newline at end of file
diff --color -ru AFLplusplus/src/afl-fuzz-one.c afl-mm-depend/src/afl-fuzz-one.c
--- AFLplusplus/src/afl-fuzz-one.c	2024-11-02 13:27:38
+++ afl-mm-depend/src/afl-fuzz-one.c	2024-11-02 13:41:14
@@ -1993,7 +1993,6 @@
 #endif
 
   if (unlikely(afl->custom_only)) {
-
     /* Force UI update */
     show_stats(afl);
     /* Skip other stages */
@@ -2016,7 +2015,6 @@
                      8;
 
   } else {
-
     perf_score = orig_perf;
 
     snprintf(afl->stage_name_buf, STAGE_BUF_SIZE, "splice %u", splice_cycle);
@@ -2039,7 +2037,6 @@
     LIST_FOREACH(&afl->custom_mutator_list, struct custom_mutator, {
 
       if (el->stacked_custom && el->afl_custom_havoc_mutation_probability) {
-
         el->stacked_custom_prob =
             el->afl_custom_havoc_mutation_probability(el->data);
         if (el->stacked_custom_prob > 100) {
@@ -2141,9 +2138,9 @@
   stack_max = 1 << (1 + rand_below(afl, afl->havoc_stack_pow2));
 
   // + (afl->extras_cnt ? 2 : 0) + (afl->a_extras_cnt ? 2 : 0);
+  // havoc only once.
+  // for (afl->stage_cur = 0; afl->stage_cur < afl->stage_max; ++afl->stage_cur) {
 
-  for (afl->stage_cur = 0; afl->stage_cur < afl->stage_max; ++afl->stage_cur) {
-
     u32 use_stacking = 1 + rand_below(afl, stack_max);
 
     afl->stage_cur_val = use_stacking;
@@ -2161,7 +2158,6 @@
 
           if (unlikely(el->stacked_custom &&
                        rand_below(afl, 100) < el->stacked_custom_prob)) {
-
             u8    *custom_havoc_buf = NULL;
             size_t new_len = el->afl_custom_havoc_mutation(
                 el->data, out_buf, temp_len, &custom_havoc_buf, MAX_FILE);
@@ -3275,7 +3271,12 @@
 
     }
 
-    if (common_fuzz_stuff(afl, out_buf, temp_len)) { goto abandon_entry; }
+    afl->record_sampling = true;
+    if (common_fuzz_stuff(afl, out_buf, temp_len)) { 
+      afl->record_sampling = false;
+      goto abandon_entry; 
+    }
+    afl->record_sampling = false;
 
     /* out_buf might have been mangled a bit, so let's restore it to its
        original size and shape. */
@@ -3288,20 +3289,20 @@
     /* If we're finding new stuff, let's run for a bit longer, limits
        permitting. */
 
-    if (afl->queued_items != havoc_queued) {
+    // if (afl->queued_items != havoc_queued) {
 
-      if (perf_score <= afl->havoc_max_mult * 100) {
+    //   if (perf_score <= afl->havoc_max_mult * 100) {
 
-        afl->stage_max *= 2;
-        perf_score *= 2;
+    //     afl->stage_max *= 2;
+    //     perf_score *= 2;
 
-      }
+    //   }
 
-      havoc_queued = afl->queued_items;
+    //   havoc_queued = afl->queued_items;
 
-    }
+    // }
 
-  }
+  // }
 
   new_hit_cnt = afl->queued_items + afl->saved_crashes;
 
@@ -6191,6 +6192,7 @@
        limit_time_sig  < 0 both are run
   */
 
+  
   if (afl->limit_time_sig <= 0) { key_val_lv_1 = fuzz_one_original(afl); }
 
   if (afl->limit_time_sig != 0) {
Only in AFLplusplus/src: afl-fuzz-python.c
Only in AFLplusplus/src: afl-fuzz-queue.c
Only in AFLplusplus/src: afl-fuzz-redqueen.c
Only in AFLplusplus/src: afl-fuzz-run.c
Only in AFLplusplus/src: afl-fuzz-skipdet.c
diff --color -ru AFLplusplus/src/afl-fuzz-state.c afl-mm-depend/src/afl-fuzz-state.c
--- AFLplusplus/src/afl-fuzz-state.c	2024-11-02 13:27:38
+++ afl-mm-depend/src/afl-fuzz-state.c	2024-11-02 13:27:20
@@ -148,6 +148,7 @@
 
   list_append(&afl_states, afl);
 
+  afl->record_sampling = false;
 }
 
 /*This sets up the environment variables for afl-fuzz into the afl_state
Only in AFLplusplus/src: afl-fuzz-stats.c
Only in AFLplusplus/src: afl-fuzz-statsd.c
diff --color -ru AFLplusplus/src/afl-fuzz.c afl-mm-depend/src/afl-fuzz.c
--- AFLplusplus/src/afl-fuzz.c	2024-11-02 13:27:38
+++ afl-mm-depend/src/afl-fuzz.c	2024-11-02 13:48:40
@@ -3424,6 +3424,10 @@
 
   destroy_queue(afl);
   destroy_extras(afl);
+
+  // write custom mutator data
+  write_custom_mutator_data(afl);
+  
   destroy_custom_mutators(afl);
   afl_shm_deinit(&afl->shm);
 
Only in AFLplusplus/src: afl-gotcpu.c
Only in AFLplusplus/src: afl-ld-lto.c
Only in AFLplusplus/src: afl-performance.c
Only in AFLplusplus/src: afl-sharedmem.c
Only in AFLplusplus/src: afl-showmap.c
Only in AFLplusplus/src: afl-tmin.c
Only in AFLplusplus/src: hashmap.c
